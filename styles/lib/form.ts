import { Style } from './helpers';
import { Palette } from './palette';

export type FormOpts = {
  palette: Palette;
};

export default function makeFormStyles({
  palette,
  spacers,
}: FormOpts): Style {
  return {};
}